/* Replace comment with updated version */
$('#comment_<%= @comment.id -%>').replaceWith("<%= escape_javascript(render(:partial => @comment.for_post? ? 'post_form' : 'form', :object => @comment)) %>");

/* Place focus on textarea */
$('#edit_comment_<%= @comment.id -%> textarea').first().focus();

$('#edit_comment_<%= @comment.id -%> textarea').autosize();

<% flash.discard %>