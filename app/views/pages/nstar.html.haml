.nstarjoe.center
  %h3 On June 4th, 2012...
  %h3 624 Teams committed to building great products started nReduce...
  %h3 Some have faltered...
  %h3 Others have risen to the challenge...
 
  %h3 On September 5 at 11am PST...
  %h3 the Best 10 will be introduced to the world and...
  %center= image_tag('nstar_final.png')
  %h2 The nStar 
  %h4 Will be Born!
  %br/
  %br/
  %br/
  %br/
  %h3{:style => 'margin: 50px 200px 0 200px; padding-bottom: 0'} Sign up to reserve a space in our Online Only Demo Day:
  %center
    = form_for @rsvp, :html => {:class => 'form-horizontal'} do |f|
      = f.hidden_field :demo_day_id
      - if @rsvp.user.blank?
        = f.text_field :email, :placeholder => 'Your email address', :class => 'email tk-myriad-pro', :style => 'font-size: 22px; height: 30px; line-height: 22px'
      - else
        = f.hidden_field :user_id
      = f.submit 'RSVP', :class => 'btn btn-primary btn-large'


.nstarold{:style => 'display: none'}
  #slide_1.slide.scrollblock
    #slide_1_bg.slide_bg{:style => 'display: inline'}
    .scroll-content
      %h1 On June 4th, 2012:
      %h2 624 Teams committed to building great products started nReduce.
  #slide_2.slide.scrollblock
    #slide_2_bg.slide_bg
    .scroll-content
      %h2 Some have faltered...
  #slide_3.slide.scrollblock
    #slide_3_bg.slide_bg
    .scroll-content
      %h2 Others have risen to the challenge.
  #slide_4.slide.scrollblock
    #slide_4_bg.slide_bg
    .scroll-content
      %h1 On September 5, the best 10 will be introduced to the world.
      %h2 and the nStar will be born!
  #slide_5.slide.scrollblock
    #slide_5_bg.slide_bg
    .scroll-content
      %h1 Sign up to reserve a space in our Online Demo Day:
      %center.rsvp
        = form_for @rsvp, :html => {:class => 'form-horizontal'} do |f|
          = f.hidden_field :demo_day_id
          - if @rsvp.user.blank?
            = f.text_field :email, :placeholder => 'Your email address', :class => 'email tk-myriad-pro'
          - else
            = f.hidden_field :user_id
          = f.submit 'RSVP', :class => 'btn btn-primary btn-large'
