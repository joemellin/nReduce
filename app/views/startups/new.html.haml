- content_for :subtitle do
  Register your startup

%h2 Register your Startup for nReduce

.row
  %div{:class => @setup ? 'span7' : 'span12'}
    = semantic_form_for @startup, :url => '/startup/', :html => {:class => "form-horizontal", :multipart => true} do |f|
      = render "startups/fields", :f => f
      .form-actions
        = f.submit @setup ? 'Continue' : 'Save', :class => "btn btn-primary btn-large"