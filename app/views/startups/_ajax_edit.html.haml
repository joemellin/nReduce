= semantic_form_for @startup, :url => '/startup/', :remote => true, :html => {:class => "form-horizontal"} do |f|
  = render 'startups/fields', :f => f
  .form-actions
    = f.submit 'Save', :class => "btn btn-primary btn-large"