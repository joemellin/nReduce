- if @requesting_entity.is_a?(Startup)
  Team #{@requesting_entity.name} would like to join your group.\n
- else
  #{@requesting_entity.name} would like to join your group.\n
  \n
  If accepted you will see each other's weekly progress and be able to help each other succeed.  It is important to build trusted relationships.\n
- unless @relationship.message.blank?
  \n
  "#{h(@relationship.message)}"
  \n
  \n
\n
View Request: #{add_teams_relationships_url}