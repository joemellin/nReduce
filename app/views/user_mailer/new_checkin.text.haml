Team #{@checkin.startup.name} has posted their 'after' video for #{@checkin.time_label}!\n
\n
- cc = @user.startup.current_checkin
- if !cc.blank? and cc.completed?
  Give Feedback & Support: startup_checkin_url(:startup_id => @checkin.startup_id, :id => @checkin.id)
- else
  Post Your Team's "After" Video: edit_checkin_url(cc)