- if @owner
  You Have Feedback!\n
- else
  #{@comment.user.name} responded to you!\n
\n
- if @owner
  #{@comment.user.name} of Team #{@comment.user.startup.name} wrote on your #{@checkin.time_label} check-in:\n
- elsif !@comment.parent.blank?
  They wrote in response to your comment "#{truncate(@comment.parent.content, :length => 30)}":\n
- else
  They wrote:
\-------------------------------------\n
"#{h(@comment.content)}"\n
\-------------------------------------\n
Respond: #{checkin_url(@checkin)}