= render :partial => 'header'
%h1.h1 Team #{@checkin.startup.name}
%br
%h4.h4 Posted their "After" Video
%br
- if !@current_checkin.blank? and @current_checkin.completed?
  %h3.h3= link_to 'Give Feedback & Support', startup_checkin_url(:startup_id => @checkin.startup_id, :id => @checkin.id)
- else
  %h4.h4 To view it and post comments...
  %h3.h3= link_to "Post Your Team's \"After\" Video", @current_checkin.new_record? ? new_checkin_url : edit_checkin_url(@current_checkin)
  
= render :partial => 'footer'